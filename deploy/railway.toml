[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/Dockerfile"

[deploy]
startCommand = "python vm.py supervise-loop --apply --interval 60"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
